# QoderGateway environment example
# Copy this file to .env or set these variables in your shell.

# Server bind address and port
QODER_HOST=127.0.0.1
QODER_PORT=5050

# Administrator password for the management console.
# Only used the FIRST time the database is created (stored in SQLite);
# afterwards the console-stored token takes effect and can be changed in the WebUI.
# Leave empty on first run to use the default "admin" and change it immediately after login.
QODER_ADMIN_PASSWORD=

# YYDS Mail API key (required by the built-in registrar, AC- prefixed).
# Get yours from the YYDS Mail console.
YYDS_API_KEY=

# Optional upstream proxy for outbound Qoder requests.
# Example: http://127.0.0.1:7890
QODER_PROXY=

# Enable or disable public static pages.
# Set to 0 / false / no / off to disable.
QODER_ENABLE_DOCUMENTS=1
QODER_ENABLE_LANDING=1

# Optional PAT auto-imported on first startup when no accounts exist.
QODER_PAT=
